/**
 * Side-effect import — registers the Shopify site adapter, tasks, and predicates
 * with the @simbench/core engine. Import this in any API route that uses the sim engine.
 */
import "./sim-adapter";
