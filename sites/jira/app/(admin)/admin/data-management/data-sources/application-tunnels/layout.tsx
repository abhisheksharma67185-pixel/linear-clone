import type { Metadata } from "next"

export const metadata: Metadata = { title: "Application Tunnels" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
