import type { Metadata } from "next"

export const metadata: Metadata = { title: "Platform Experiences" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
